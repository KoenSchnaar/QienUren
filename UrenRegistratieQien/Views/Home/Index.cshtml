
<a class="nav-link text-dark linkjes" asp-area="Identity" asp-page="/Account/Login">Login</a>

@*Misschien kunnen we onderstaande gebruiken om het dasboard te showen van het soort gebruiker die inlogt?*@

@*@using Microsoft.AspNetCore.Identity
@inject SignInManager<UrenRegistratieQien.DatabaseClasses.Employee> SignInManager
@inject UserManager<UrenRegistratieQien.DatabaseClasses.Employee> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Profiel inzien</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>*@